function handleCategoryChange() {
    var selectedCategory = document.getElementById('categorySelect').value;
    console.log('Вибрана категорія:', selectedCategory);
    // Логіка для обробки вибору категорії
  }
  